services:
  - type: web
    name: AgriNathiApp
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn wsgi:application --workers 1 --bind 0.0.0.0:$PORT --log-level debug --timeout 120 --preload
    envVars:
      - key: FLASK_ENV
        value: production
      # Add other environment variables (API keys, credentials) in Render dashboard when connecting the repo.